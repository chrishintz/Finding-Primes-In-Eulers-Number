<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Finding the Prime Numbers from Euler's Equation</title>
  </head>
  <body>
    <div class="container">
      <div class="form-header header-primary outline">
        <h1 class="euler-header align-center">Euler's Primes</h1>
        <form action="/calculate" method="POST" enctype="multipart/form-data">
          <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>" />
          <div class="form-group">
            <label class="white-text">Prime Number</label>
            <input class="form-control" type="number" name="prime_number" placeholder="What number of prime are you looking for?"></input>
          </div>
          <div class="form-group">
            <label class="white-text">Number of Digits</label>
            <input class="form-control" type="number" name="digits" placeholder="How many digits should the number be?"></input>
          </div>
          <div class="form-group">
            <input id="fileSelect" type="file" accept=".csv" name="csv" />
          </div>
          <div class="form-group align-center">
            <input id="submit-button" type="submit" value="Calculate" class="calculate" />
          </div>
        </form>
        <% if @euler %>
          <div class="result align-center full-width">
            <h2 class="white-text">Result(s)</h2>
            <ul class='list-group'>
              <% @results.each do |result| %>
                <li class="list-group-item align-center results">
                  <%= result %>
                </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>
